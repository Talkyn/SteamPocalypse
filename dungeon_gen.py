import settings



"""
if __name__ == "__main__":
    import libtcodpy as tcod
    
    tcod.console_init_root(settings.SCREEN_W, settings.SCREEN_H, 'SP DungeonGen Tests')
    tcod.sys_set_fps(20)
    
    while not tcod.console_is_window_closed():
        # Render the dungeon
"""